import LiveMatchTopCard from "./cards/LiveMatchTopCard";
import ScoringCard from "./cards/ScoringCard";

const TOP_TAB_COMPONENTS = [
  { name: "Live Match Card", component: LiveMatchTopCard },
  { name: "Scoring Card", component: ScoringCard },
];

const Components = {
  TOP_TAB_COMPONENTS,
};

export default Components;
